<Application x:Class="AIBar.Desktop.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:system="clr-namespace:System;assembly=mscorlib"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <FontFamily x:Key="AppFont">Segoe UI</FontFamily>
        <system:Double x:Key="SpacingSmall">8</system:Double>
        <system:Double x:Key="SpacingMedium">16</system:Double>
        <CornerRadius x:Key="CardRadius">10</CornerRadius>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFF7F7F7" />
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFFFF" />
        <SolidColorBrush x:Key="TextBrush" Color="#FF1B1B1B" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#FF545454" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FF0078D4" />
        <SolidColorBrush x:Key="FocusBrush" Color="#FF005FB8" />
        <SolidColorBrush x:Key="HighContrastBorderBrush" Color="{DynamicResource {x:Static SystemColors.WindowTextColorKey}}" />

        <Style x:Key="KeyboardFocusVisual" TargetType="{x:Type Control}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border BorderBrush="{DynamicResource FocusBrush}" BorderThickness="2" CornerRadius="3" Margin="-2" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="QuotaCardStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="{DynamicResource CardRadius}" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                    <Setter Property="TextElement.Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
<Style x:Key="QuotaCardSecondaryTextStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
                <Style.Triggers><DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True"><Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" /></DataTrigger></Style.Triggers>
            </Style>
            <Style TargetType="{x:Type Button}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource KeyboardFocusVisual}" />
            <Setter Property="Padding" Value="12,6" />
        </Style>
    </Application.Resources>
</Application>
